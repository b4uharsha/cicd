---
  roles:
    - src: geerlingguy.nginx
      version: "3.0.0"
  
  collections:
    - name: community.general
      version: ">=2.0.0"
  
  collections:
    - name: google.cloud
      version: ">=1.0.0"
        