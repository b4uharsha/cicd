pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                // Checkout the source code from GitHub
                checkout scm
            }
        }

        stage('Build') {
            steps {
                echo 'Building the project...'
                // Add your build steps here, like 'sh "mvn clean install"' or 'npm install'
            }
        }

        stage('Test') {
            steps {
                echo 'Running tests...'
                // Add test execution steps, e.g., 'sh "mvn test"' or 'npm test'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying the project...'
                // Add deployment steps, like copying to servers or deploying to cloud services
            }
        }
    }
}

